# ✅ PRONTO PRA DEPLOY

Logins realizados com sucesso:
- Railway: ✓ `vitor.e.jesus@aluno.senai.br`
- Vercel: ✓ `vitorejesus-oss`

## Próximo passo (Claude Code)

- `railway up` (backend) — cria/associa um projeto no Railway e sobe o `app.py`
- `vercel --prod` (frontend) — sobe o build de produção do `frontend/`

**Antes de disparar isso: é um deploy real, público.** Ainda vale a pena
trocar a senha demo (`123456`) e revisar o `.env.production` antes de ir ao
ar — ver `DEPLOY_PRODUCAO.md` para os detalhes e os dois avisos técnicos
(SQLite efêmero no Railway, CORS_ORIGINS).

Esperando confirmação do usuário...
